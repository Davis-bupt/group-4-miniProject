spring:
  application:
    name: deskflow-api

  datasource:
    url: jdbc:h2:mem:deskflow;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password: ""

  jpa:
    hibernate:
      # none = Hibernate does not create/alter tables. Schema is owned by the
      # data lead via schema.sql (+ data.sql for seed), run by Spring on startup.
      ddl-auto: none
    open-in-view: false
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  h2:
    console:
      enabled: true
      path: /h2-console

server:
  port: 8080

springdoc:
  swagger-ui:
    path: /swagger-ui.html
